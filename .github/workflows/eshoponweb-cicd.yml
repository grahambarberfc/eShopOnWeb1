name: eShopOnWeb Build and Test

on:
  workflow_dispatch:

env:
  RESOURCE-GROUP: rg-eshoponweb-westeurope
  LOCATION: westeurope
  TEMPLATE-FILE: infra/webapp.bicep
  SUBSCRIPTION-ID: "2c62cd7a-008b-4843-8277-9aefd6e1b230"
  WEBAPP-NAME: devops-webapp-westeurope-56351720

jobs:
  buildandtest:
    runs-on: ubuntu-latest
    steps:
      # 1️⃣ Checkout repository
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Setup .NET 9 SDK (Stable channel)
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'
          cache: false  # set true after you add lock files

      # 3️⃣ Clean previous builds
      - name: Clean solution
        run: dotnet clean ./eShopOnWeb.sln

      # 4️⃣ Restore dependencies
      - name: Restore dependencies
        run: dotnet restore ./eShopOnWeb.sln --force-evaluate

      # 5️⃣ Build solution
      - name: Build with dotnet
        run: dotnet build ./eShopOnWeb.sln --configuration Release --no-restore

      # 6️⃣ Run tests (now with .NET 9 assemblies)
      - name: Test with dotnet
        run: dotnet test ./eShopOnWeb.sln --configuration Release --no-build --verbosity normal

      # 7️⃣ Publish output for deployment
      - name: dotnet publish
        run: |
          dotnet publish ./src/Web/Web.csproj -c Release -o ${{env.DOTNET_ROOT}}/myapp
          cd ${{env.DOTNET_ROOT}}/myapp
          zip -r ../app.zip .

      # 8️⃣ Upload web app artifact
      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: .net-app
          path: ${{env.DOTNET_ROOT}}/app.zip

      # 9️⃣ Upload Bicep template
      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: bicep-template
          path: ${{ env.TEMPLATE-FILE }}

  deploy:
    runs-on: ubuntu-latest
    needs: buildandtest
    environment:
      name: 'Development'

    steps:
      # 🔽 Download app artifact
      - name: Download app artifact
        uses: actions/download-artifact@v4
        with:
          name: .net-app
          path: .net-app

      # 🔽 Download Bicep template
      - name: Download Bicep template
        uses: actions/download-artifact@v4
        with:
          name: bicep-template
          path: bicep-template

      # 🔐 Azure Login
      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      # ☁️ Deploy Infrastructure using Bicep
      - name: Deploy infrastructure
        uses: azure/arm-deploy@v2
        with:
          subscriptionId: ${{ env.SUBSCRIPTION-ID }}
          resourceGroupName: ${{ env.RESOURCE-GROUP }}
          template: bicep-template/webapp.bicep
          parameters: 'webAppName=${{ env.WEBAPP-NAME }} location=${{ env.LOCATION }}'
          failOnStdErr: false

        # 🚀 Deploy Website using Azure CLI (ZIP deploy)
      - name: Publish Website to WebApp
        uses: azure/cli@v2
        with:
          inlineScript: |
            echo "🔧 Setting runtime stack to .NET 9 (Linux)..."
            az webapp config set \
              --resource-group ${{ env.RESOURCE-GROUP }} \
              --name ${{ env.WEBAPP-NAME }} \
              --linux-fx-version "DOTNETCORE|9.0"
    
            echo "🚀 Deploying app to Azure WebApp..."
            az webapp deploy \
              --name ${{ env.WEBAPP-NAME }} \
              --resource-group ${{ env.RESOURCE-GROUP }} \
              --src-path .net-app/app.zip \
              --type zip

